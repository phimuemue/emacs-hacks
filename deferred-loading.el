;; i have a couple of things in emacs i don't want to have loaded
;; automatically at startup, but that i want to be able to load
;; on demand.

;; Therefore, this file encapsulates those functions whose purpose
;; is just to provide these facilities.

